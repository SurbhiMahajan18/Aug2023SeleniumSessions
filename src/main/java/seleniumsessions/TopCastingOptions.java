package seleniumsessions;

import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chromium.ChromiumDriver;
import org.openqa.selenium.edge.EdgeDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.remote.RemoteWebDriver;

public class TopCastingOptions{

	public static void main(String[] args) {
		//ChromeDriver driver = new ChromeDriver();
		//WebDriver driver = new WebDriver();
		//SearchContext sc = new ChromeDriver();valid -not recommended because only two methods are avaiilable(findElement and findElements)
		//RemoteWebDriver driver =new ChromeDriver();
		//RemoteWebDriver driver1=new RemoteWebDriver();//mostly used to run tests on remote machine/cloud
		//driver.get("https://google.com");
		//ChromiumDriver driver = new ChromeDriver();//valid and only for two browsers chrome and edge
		//ChromiumDriver driver = new EdgeDriver();//valid
		//ChromiumDriver driver = new FirefoxDriver();//invalid ofcourse
		

	}

}
